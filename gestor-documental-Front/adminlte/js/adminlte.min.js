/* AdminLTE 3.2.0 simplificado para el proyecto */
(function(global, factory) {
    "use strict";
    if (typeof module === "object" && typeof module.exports === "object") {
        module.exports = global.document ?
            factory(global, true) :
            function(w) {
                if (!w.document) {
                    throw new Error("AdminLTE requires a window with a document");
                }
                return factory(w);
            };
    } else {
        factory(global);
    }
})(typeof window !== "undefined" ? window : this, function(window, noGlobal) {
    "use strict";

    // AdminLTE utilities
    var AdminLTE = {
        // Initialize AdminLTE
        init: function() {
            this.initSidebar();
            this.initNavbar();
        },
        
        // Initialize sidebar
        initSidebar: function() {
            var sidebar = document.querySelector('.sidebar');
            if (sidebar) {
                // Add click handlers for sidebar links
                var links = sidebar.querySelectorAll('nav a');
                links.forEach(function(link) {
                    link.addEventListener('click', function(e) {
                        // Remove active class from all links
                        links.forEach(function(l) {
                            l.classList.remove('active');
                        });
                        // Add active class to clicked link
                        this.classList.add('active');
                    });
                });
            }
        },
        
        // Initialize navbar
        initNavbar: function() {
            var navbar = document.querySelector('.navbar');
            if (navbar) {
                // Add navbar functionality here
            }
        }
    };

    // Initialize AdminLTE when DOM is ready
    if (document.readyState === 'loading') {
        document.addEventListener('DOMContentLoaded', function() {
            AdminLTE.init();
        });
    } else {
        AdminLTE.init();
    }

    // Global AdminLTE
    if (!noGlobal) {
        window.AdminLTE = AdminLTE;
    }

    return AdminLTE;
});
